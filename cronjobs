# 根据 BACKUP_INTERVAL 环境变量设置备份频率
# 每分钟执行一次（测试用）
* * * * * /usr/local/bin/backup.sh >> /var/log/cron.log 2>&1

# 每天凌晨2点执行（生产环境使用）
# 0 2 * * * /usr/local/bin/backup.sh >> /var/log/cron.log 2>&1

# 每小时执行一次
# 0 * * * * /usr/local/bin/backup.sh >> /var/log/cron.log 2>&1

# 每周日凌晨2点执行
# 0 2 * * 0 /usr/local/bin/backup.sh >> /var/log/cron.log 2>&1